package sv.gob.mh.siip.model.administracion.enums;

/** Estado de un {@code Calendario} (CU-ADM-04, RN20): nunca se elimina, solo transiciona entre estos dos valores. */
public enum EstadoCalendario {
    ACTIVO,
    INACTIVO
}
